<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.a2zbilling">

    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ConformList"
            android:theme="@style/AppTheme.ActionBar"></activity>
        <activity android:name=".WaitList"
            android:theme="@style/AppTheme.ActionBar"/>
        <activity
            android:name=".AddToCard"
            android:theme="@style/AppTheme.ActionBar"
            android:parentActivityName=".MainActivity"/>
        <activity android:name=".DefaultItemList" />
        <activity android:name=".AddItemFloatingButton" />
        <activity
            android:name=".AddItem"
            android:parentActivityName=".MainActivity"
            android:theme="@style/AppTheme.ActionBar" />
        <activity android:name=".Customer" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>